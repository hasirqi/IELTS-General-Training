# 破壁 IELTS 6：详细项目计划书

> 本计划书描述下一阶段的实际交付顺序。背景、共同决策和术语见 [项目介绍](PROJECT_OVERVIEW.md)。

## 当前起点

已经完成 4,133 条正式教学词与词块、听说读写各 128 节共 512 节、1,024 道听力与阅读客观题、128 段固定英式 MP3、192 条句型挑战、192 条开口任务、36 周课程路线和 301 项自动测试。

雅思核心词扩展 16 批已经结束，不再生成第 17 批。

当前工作区存在未跟踪实验材料：旧 10K 表层词频文件、构建脚本、阅读题库实验和阅读能力引擎实验。这些只代表探索结果，不代表正式完成。没有通过来源、结构、内容和算法门禁前，不得直接接入正式 UI 或计入完成数。

## 实时执行状态（2026-08-01）

- 当前里程碑：M4 文本难度引擎，V0 特征和首批 120 篇合法来源候选已完成；两名隔离 AI 评审与第三 AI 裁决已完成，明确不冒充真人标签。
- M3 600 锚点可用版已完成：内容题库、36 组路径模拟、重测回避、频段曝光、高能力上限探测、异常可信度和 500 词族粒度的 90% 区间都已通过门禁。
- 下一工作单元：补充真正覆盖 L1、L5、L6 的合法来源文本，优先填补当前 L1=5、L5=9、L6=0 的缺口；六层各至少 20 篇前不得训练或发布六层模型。
- M1 实际结果：20,000 个唯一候选词族；4,133 条教学词全部有处理结果，其中直接命中 3,187、词族成员命中 121、词块组件映射 121、明确未匹配 704。
- 旧 10K 实验文件已确认是表层词形排名且缺少随附原始来源文件，继续保持实验状态，不接入正式 UI。
- 20K 词族索引仍保持 `frequency-seeded` 底座；仅 600 个完成人工题目门禁的 `item-authored` 锚点可进入实验计分。
- M2 实际结果：15 题 Yes/No 路由（含 3 个非词）、英文语境释义 CAT、20–30 道正式计分题、1PL/2PL 兼容 EAP、可信度检查、IndexedDB 恢复和三层测试中心均已接入现有入口。
- 扩词状态：16 批全部完成，正式教学词库 4,133 条，不再生成第 17 批。

## 最终目标

- 20,000 英语词族后台索引。
- 150 锚点引擎验证版。
- 600 锚点正式可用版。
- 1,000 锚点完整平台。
- 720 道正式等级测评题。
- 60 篇功能性短文。
- 50 篇连续阅读文章。
- 六个难度层。
- 三套以上轮换路径。
- 词汇 CAT 30–45 题、约 12–18 分钟。
- 阅读 CAT 38–50 题、约 20–30 分钟。
- 每 4–6 周可重测并回避近期题目。
- 输出教学词掌握、接受性词族、内部模拟阅读值和 GT Reading 准备度。

## 不可破坏的边界

- 不把内部结果称为 MetaMetrics 官方 Lexile 或蓝思认证。
- 不把接受性词汇量称为写作或口语产出能力。
- 不用词汇量直接换算 IELTS 总分。
- 不把官方 VST 与自研 CAT 做简单比例换算。
- 不复制无再发布许可的官方题目或频率数据到公开仓库。
- 不让未人工审核锚点进入正式计分。
- 没有群体数据时不伪造 2PL 区分度。
- AI 不决定正确答案，也不自由改变测量范围。
- 个人数据默认只保存在 IndexedDB。

## 里程碑总览

| 里程碑 | 状态 | 目标 | 用户可见结果 |
|---|---|---|---|
| M0 | 已完成 | 固化方案和续作入口 | 项目文档 |
| M1 | 已完成 | 20K 词族测量底座 | 暂不显示 |
| M2 | 已完成 | 150 锚点与 CAT 垂直切片 | 实验层级定位 |
| M3 | 已完成 | 600 锚点可用版与 CAT 算法门禁 | 带可信度的内部接受性估计 |
| M4 | 进行中 | V0 特征＋120 篇AI独立复核种子；待补齐六层覆盖和模型比较 | 暂不显示分数 |
| M5 | 待开始 | 六批 720 题与 110 篇文章 | 正式等级题库 |
| M6 | 待开始 | 阅读 CAT 与三条路径 | 内部模拟阅读值 |
| M7 | 待开始 | 三轴结果与学习回流 | 完整能力坐标 |
| M8 | 待开始 | 1,000 锚点完整 20K 平台 | 高阶上限探测 |
| M9 | 待授权 | 外部区间校验和 GT 映射研究 | 观察性考试区间 |

每个正式里程碑独立提交、推送、等待 GitHub Actions，并核对线上资源。

## M0：文档和实验材料审计

交付：PROJECT_OVERVIEW.md、PROJECT_PLAN.md、更新 README.md 和 AGENTS.md，并建立实验材料清单。

验收：所有项目数字一致；10K 旧实验不再被描述为正式词族索引；下一终端能找到第一个未完成步骤；不误提交用户本地审核材料。

## M1：20K 词族测量底座

### M1.1 定义词族规则

先定义并用测试样例固定：词族代表词、屈折变化、透明与不透明派生、多词词块、专有名词、数字日期、缩写、拼写变体和同形异义。

### M1.2 正式字段

每个词族至少记录：familyId、headword、members、frequencyRank、frequencyBand、partsOfSpeech、syllables、ageOfAcquisition、concreteness、derivationType、properNoun、numberLike、teachingLexiconIds、source、licence、reviewStatus 和 version。

### M1.3 来源与许可证

对每个来源记录名称、版本、下载地址、原作者、许可证、是否允许修改、是否允许公开再发布、是否只允许研究使用，以及项目实际保存原始内容还是派生统计。

### M1.4 映射 4,133 教学词

输出映射成功数、一词多族歧义、多词归并同族、专有名词、数字、透明派生、各频率层覆盖率和未匹配清单。

### M1.5 门禁

- 正好 20,000 个唯一 familyId。
- 频率排名连续。
- 每个词族只有一个主层级。
- 透明派生不重复膨胀。
- 来源与许可证不为空。
- 4,133 教学词都有明确处理结果。
- 自动测试覆盖归并、特殊词、重复和统计。

Git 节点：Build 20K word-family measurement index。

## M2：150 锚点复合词汇 CAT 垂直切片

状态：已完成并发布（提交 `76cf9f4`）。

### 锚点与题型

- 1K–5K 每层 24 个，6K–8K 每层 10 个，共 150 个。
- 每题包含词族、频率层、词性、聚焦中文义、真实英文语境、项目原创英文释义、三个同词性英文干扰项、三个中文干扰项、初始参数和审核记录。
- 正式词汇 CAT 使用“英文关键词＋英文短语境＋四选一英文释义”。
- 英中四选一降级为每日快速自测，不进入正式结果。

### 基础路由与可信度

- 固定 15 题：12 个覆盖 1K–5K 的真实词＋3 个非词。
- Yes/No 只确定 CAT 起点和可信度，不进入词汇量分数。
- 非词不计分、不进入错词；误认多个非词会降低结果可信度。

### 浏览器端引擎

1PL/EAP 正式运行，数据接口兼容未来 2PL。正式 CAT 最少 20 题、最多 30 题；达到标准误差和频段覆盖后停止，18 分钟强制停止。草稿与结果由 IndexedDB 保存，旧版未完成草稿不会混入新规则。

### 测试中心

采用三层结构：紧凑推荐卡、五种并列模式、最近结果摘要。五种模式均已可操作并使用独立随机题池：快速定位、词汇量精测和每日词汇自测沿用各自测量边界；阅读能力测评从 128 篇已审核 GT 阅读课程中每轮随机抽 6 篇共 24 题；语境运用从已审核词条例句中每轮随机抽 20 道完形。后两者当前只给正确数和分项练习结果，720 题、110 篇文章和双人难度标定通过后才开放标准化阅读值。

结果只报告宽频率层、区间、每层表现和可信度，不显示伪精确词数、官方蓝思认证、IELTS 换算或无样本支持的群体百分位。

Git 节点：Add 150-anchor contextual vocabulary CAT pilot。

## M3：600 锚点正式可用版

状态：已完成。600 个锚点全部为 `item-authored`，候选为 0；36 组 route-first、direct CAT 和近期重测模拟通过 80% 一致性门禁。路由先验只影响起点并随正式题衰减；高能力结果未完成 9K–20K 上限探测时不允许提前停止。技术 guardrail 不直接堆到 UI，只转换为用户可理解的可信度原因。

### 分布

- 1K–8K：每层 60，共 480。
- 9K–14K：每层 15，共 90。
- 15K–20K：每层 5，共 30。

### 内容顺序

按 1K–2K、3K–4K、5K、6K–8K、9K–14K、15K–20K 分批审核。

每批检查层级顺序、词族唯一、词性平衡、答案唯一、干扰项有效、中文自然度、跨批重复、伪词冲突、来源和许可证。

候选清单由 `scripts/build-vocabulary-anchor-candidates-600.mjs` 从 20K 词族索引、教学词映射和已审核教学语境确定性生成，产物为 `src/content/vocabulary-anchor-candidates-600.json`。生成器只接受一对一直接映射、唯一词族、目标词可在例句和搭配中核验的教学词，并按频段内目标分位点抽样，避免候选集中在频段起点。候选文件明确设置 `scoringEligible: false`，运行时 CAT 不得导入它。

### 正式输出

词族量点估计、置信区间、1K–20K 分层画像、可信度、与 4,133 教学词的交集和需要进入学习的高价值缺口。文案标记为“内部预标定接受性词族估计”。

### 重测

默认 4–6 周，保存近期题目，优先未见锚点；必要锚题可有限重复；过于频繁重测降低可信度。

完成节点：Release 600-anchor vocabulary assessment。

## M4：文本难度引擎

### V0 特征

实现文本清洗与句子识别、词族切分、平均句长及对数、平均词频对数、各频率层覆盖率、未登录词、音节和长词、从句和连接词、代词指代、名词化、抽象度、信息密度及词汇句法多样性。

### 六个内部层

- L1：基础识别和短句。
- L2：生活功能文本。
- L3：多段生活与工作文本。
- L4：复杂信息与一般推断。
- L5：抽象观点和较密集论证。
- L6：高级连续文本。

### 标定语料

目标至少 600 篇合法参考文本，每层约 100 篇，覆盖生活、工作、教育、公共服务、说明和论证。保存人工标签、来源和许可；不能再发布的文本只保存允许的特征、链接和哈希。训练、验证和最终保留测试集必须分离。

### 模型比较

比较透明线性基线、Ridge/Elastic Net 和轻量 GBDT，记录 MAE/RMSE、分层混淆、极端误差、特征重要性和版本。复杂模型没有明显提升时保留透明模型。

### 现有课程回标

给 128 节阅读课程计算内部难度、词族覆盖、关键生词和句法挑战，并人工抽查每层代表文本。发现倒挂应修正模型，不能手工改一个数字掩盖问题。

Git 节点：Add auditable internal text difficulty engine。

## M5：六批 720 题与 110 篇文章

每个难度层一个批次，共 120 题：40 道英文语境词义题（英文语境＋英文释义）、20 道完形、10 篇功能文本共 30 题、8–9 篇连续文章共 30 题。英中题与 Yes/No 路由不占 720 道正式计分题。

六批完成后正好达到 720 题、60 篇功能文本和 50 篇连续文章。

主题覆盖生活服务、住房、交通、健康、工作、培训、安全、消费、投诉、公共服务、社区、环境、社会议题、科普和一般抽象论证。

每篇人工标注关键生词、必须理解词、可推断词、低价值低频词、搭配、长句、主干、指代链、连接关系、题目证据和干扰项错因。

每题必须有唯一 ID、四个唯一选项、唯一答案、明确证据、自然英文题干、中文证据解释，不跨批重复、不含占位符。文本难度和题目难度分开保存。

六个批次分别测试、构建、提交、推送、等待部署并核对线上内容。

## M6：阅读 CAT 与三条路径

路径 A 为生活服务和公共信息；路径 B 为工作、培训和社区事务；路径 C 为消费、健康、环境和社会议题。共同锚题用于比较，主体文章不重复。

选题规则：文章成组出现；先中间难度定位；根据答案上下探；每次至少 2 篇功能文本和 3 篇连续文章；事实、主旨、指代、推断和篇章关系保持最低覆盖；回避近期文章。

停止规则：最少 38 题、最多 50 题、最长 30 分钟、覆盖至少 3 个难度层；初期 SE ≤ 0.35 theta 才允许提前结束；未收敛时给宽区间和低置信度。

结果输出内部模拟阅读值、区间、功能性与连续阅读表现、事实主旨指代推断篇章表现、速度和可信度。

Git 节点：Release adaptive reading assessment。

## M7：三轴能力坐标与学习回流

结果页同时显示教学词掌握、接受性词族及区间、内部模拟阅读值及区间、GT Reading 准备度、置信度和参数版本。

诊断至少支持：词汇低阅读低、词汇高阅读低、词汇一般阅读较高、阅读高而 IELTS 题型低、多个结果矛盾。

错误回流：生词进入复习；词义混淆进入词义实验室；长句误解进入句型实验室；主旨指代推断进入阅读专项；时间不足进入速度训练；IELTS 证据定位错误进入题型训练。

阅读护照显示内部文本难度、用户能力、已知覆盖、舒适/训练/冲刺、关键生词和篇章挑战。

Git 节点：Connect vocabulary reading and IELTS ability map。

## M8：完整 1,000 锚点

补齐 1K–8K 每层 80、9K–14K 每层 40、15K–20K 每层 20。

验证高能力端不提前封顶、低能力端不被生僻词污染、4–6 周重测仍有未见题、路径结果重叠和锚题曝光合理。

完成节点：Complete 20K vocabulary measurement platform。

## M9：外部区间校验与 IELTS 研究

用户可以手动录入官方 VST 结果，只比较区间是否重叠，不自动修改自研参数。允许本地录入 GT Reading 分数、考试日期和内部测试间隔，只做个人纵向观察。

群体研究只有另行授权才启动，届时需要自愿参加、隐私和删除机制、后端访问控制、样本计划、共同锚题、Rasch/2PL、DIF、外部效标和交叉验证。样本不足时不发布精确 IELTS 换算。

## 全程测试体系

### 数据测试

20K 词族唯一、频率层连续、透明派生处理、专有名词数字处理、锚点分布、状态门禁和许可证字段。

### 内容测试

答案与干扰项唯一、文章题干不重复、证据存在、无占位符、词性匹配、中文自然、主题和层级覆盖。

### 算法测试

单调性、随机猜测、全对全错、边界能力、高层偶然猜对、标准误差收敛、路径等价、重测回避、曝光控制和参数版本兼容。

### 产品测试

IndexedDB 保存迁移、中断恢复、离线、键盘、触控、屏幕阅读器、字号、iPhone、iPad、Windows、macOS 和 PWA 更新。

### 发布测试

每个节点依次执行专属测试、全量 pnpm test、pnpm build、git diff --check、明确暂存、提交推送 main、等待 GitHub Actions、核对线上版本和资源。

## 参数和结果版本

每次结果保存 engineVersion、wordFamilyIndexVersion、itemBankVersion、calibrationVersion、routeId、开始结束时间、theta、standardError 和 confidence。

模型更新不静默改写旧结果。保留原始答案；如用新模型重算，必须明确标记“重算”。

## 主要风险

- 数据版权：先审计再入库；个人非商业使用不等于可公开再发布。
- 假精度：参数状态明确区分词频初始、专家审核、实验和稳定标定。
- 内容量过大：按 150、600、1,000 锚点和六个文章层分批。
- 单用户无法群体标定：先用 1PL 和宽区间，2PL 群体标定另行授权。
- 测试疲劳：词汇和阅读拆分，允许保存草稿。
- 题目记忆：扩大题池、三条路径、4–6 周回避和曝光控制。
- 词汇量冒充英语能力：三轴分开并将错误回流到真正薄弱模块。

## 下一次立即执行

下一个终端继续 M4 的层级覆盖修复，不再修改 M3 的 600 锚点分布：

1. 当前 120 篇 AI 独立复核种子为 L1=5、L2=34、L3=40、L4=32、L5=9、L6=0；一致 99、第三方裁决 21、未解决 0。
2. 从许可清楚的公共领域或允许再发布来源补充至少 15 篇 L1、11 篇 L5 和 20 篇 L6 候选；为抵消复核降级，实际采集量应高于最低缺口。
3. 新文本继续使用两套隔离 AI 评审和第三 AI 裁决，保存 `aiLabels` 与 `independent-ai-review-v1`，不得写入 `humanLabels`。
4. 六层各至少 20 篇且文本清洗、来源、许可、哈希和未登录词检查通过后，才比较透明线性基线、Ridge/Elastic Net 和轻量 GBDT。
5. 模型比较必须保持 train／validation／holdout 隔离，报告 MAE、RMSE、层级混淆和极端误差；L6 缺失时门禁必须失败。
6. 当前全部记录保持 `scoreEligible: false`，测试中心继续只显示真实练习正确数，不显示模拟 L 值。

任何终端找到本节，即可知道当前下一步。

## 2026-07-27 工作日志
- 2026-08-01 完成首批 120 篇隔离 AI 复核：Reviewer A／B 独立完成全部文章，99 篇一致、21 篇由第三 AI 裁决、未解决 0；最终分布 L1=5、L2=34、L3=40、L4=32、L5=9、L6=0。数据明确使用 `aiLabels`、`ai-label-reviewed` 和 `independent-ai-review-v1`，人工标签仍为 0、计分合格仍为 0。由于 L6 为 0，六层模型门禁保持关闭。自动测试增至 301 项。
- 2026-08-01 修复测试中心不可用和重复抽题：开放阅读能力测评与语境运用练习，分别每轮随机抽取 6 篇已审核 GT 阅读课程文章共 24 题、20 道已审核词条例句完形；每日英中自测每轮随机 20 题；快速定位每轮生成新随机种子；词汇 CAT 在能力信息优先的候选中加入会话随机扰动并随机选项顺序。五种模式同轮不重复，新开一轮换题，只有继续草稿保持原题。阅读与语境暂不输出模拟 L 值。浏览器实测五个入口、自动下一题和连续重开换题均通过；自动测试增至 296 项。
- 2026-08-01 完成 M4 双盲评审基础设施：生成 Reviewer A／B 两套不同顺序且覆盖同一 120 篇的清单；统一 L1–L6 判定量表、置信度和证据代码；提交门禁检查版本、完整性、唯一性和评审身份；一致项可进入 `label-reviewed`，分歧必须由第三位不同真人裁决，禁止自动取平均。盲审材料不包含机器候选层、文本特征、固定分割、来源等级或另一人的答案。当前真实人工标签仍为 0、计分合格仍为 0。自动测试增至 291 项。
- 2026-08-01 关闭 M3 算法门禁：修正高能力用户未上探 9K–20K 即提前停止的缺口；路由先验随正式语境题指数衰减；新增 36 组多路径与重测模拟；近期题重复、覆盖不足、频段集中和上限探测不足会降低可信度。可信结果开放 500 词族粒度的点估计和 90% 区间，低可信结果仅显示宽层级。自动测试增至 274 项。
- 2026-07-28 接入 600 锚点 CAT guardrail 首批算法门禁：`selectNextVocabularyAnchor` 支持近期锚点回避、单频段曝光上限和高能力 9K–20K 上限探测；`buildVocabularyPilotResult` 输出 1K–20K `bandProfile`、`sampledAnchorIds` 和 `guardrails` 摘要；新增路径一致性比较函数，确认 route-first 与 direct CAT 结果不输出官方认证字段。自动测试增至 269 项。
- 2026-07-28 完成 5 个 20K 人工锚点，覆盖 `amphibious` 至 `disseminate`，补齐该频段 5 条目标；手工编写 15 个同词性英文干扰项。`amphibious` 按两栖或水陆两用形容词处理，`reimburse` 按报销或偿还动词处理，`ostrich` 按鸵鸟名词处理，`intrepid` 按勇敢无畏形容词处理，`disseminate` 按传播或散布动词处理。正式题库更新为 `vocabulary-anchor-bank-600.json`；600 锚点内容题库完成，剩余候选 0。
- 2026-07-28 完成 5 个 19K 人工锚点，覆盖 `trespass` 至 `callous`，补齐该频段 5 条目标；手工编写 15 个同词性英文干扰项。`trespass` 按擅自进入动词处理，`plunder` 按掠夺动词处理，`interlude` 按插曲或间歇名词处理，`heterogeneous` 按异质或多样构成形容词处理，`callous` 按冷酷无情形容词处理。正式题库更新为 `vocabulary-anchor-bank-595.json`；剩余 5 条 20K 候选继续禁止计分。
- 2026-07-28 完成 5 个 18K 人工锚点，覆盖 `drizzle` 至 `backfire`，补齐该频段 5 条目标；手工编写 15 个同词性英文干扰项。`drizzle` 按毛毛雨名词处理，`asymmetric` 按不对称形容词处理，`blunder` 按严重错误名词处理，`brunt` 按主要冲击名词处理，`backfire` 按产生反效果动词处理。正式题库更新为 `vocabulary-anchor-bank-590.json`；剩余 10 条 19K–20K 候选继续禁止计分。
- 2026-07-28 完成 5 个 17K 人工锚点，覆盖 `ethereal` 至 `foresight`，补齐该频段 5 条目标；手工编写 15 个同词性英文干扰项。`ethereal` 按轻盈超凡处理，`impromptu` 按即兴或临时处理，`howl` 按动词嚎叫或呼啸处理，`footnote` 按脚注或补充说明处理，`foresight` 按远见或预见处理。正式题库更新为 `vocabulary-anchor-bank-585.json`；剩余 15 条 18K–20K 候选继续禁止计分。
- 2026-07-28 完成 5 个 16K 人工锚点，覆盖 `lament` 至 `jingle`，补齐该频段 5 条目标；手工编写 15 个同词性英文干扰项。`lament` 按哀叹或惋惜处理，`luminous` 按发光或明亮处理，`itinerary` 按行程安排处理，`intangible` 按无形或难以量化处理，`jingle` 按广告短歌或叮当声处理。正式题库更新为 `vocabulary-anchor-bank-580.json`；剩余 20 条 17K–20K 候选继续禁止计分。
- 2026-07-28 完成 5 个 15K 人工锚点，覆盖 `enrich` 至 `morbid`，补齐该频段 5 条目标；手工编写 15 个同词性英文干扰项。`enrich` 按丰富或改善处理，`irreversible` 按不可逆转处理，`monologue` 按独白或长篇独谈处理，`morbid` 按病态或与死亡有关处理。正式题库更新为 `vocabulary-anchor-bank-575.json`；剩余 25 条 16K–20K 候选继续禁止计分。
- 2026-07-28 完成 15 个 14K 人工锚点，覆盖 `discredit` 至 `mammal`，补齐该频段 15 条目标；手工编写 45 个同词性英文干扰项。`discredit` 按使名誉受损或使不可信处理，`conjecture` 按名词推测处理，`ignite` 按点燃或着火处理，`hinge` 按取决于处理，`bolster` 按增强或支持处理。正式题库更新为 `vocabulary-anchor-bank-570.json`；剩余 30 条 15K–20K 候选继续禁止计分。
- 2026-07-28 完成 15 个 13K 人工锚点，覆盖 `betray` 至 `transient`，补齐该频段 15 条目标；手工编写 45 个同词性英文干扰项。`betray` 按暴露或流露处理，`embark` 按开始或着手处理，`posh` 按豪华或上流处理，`longitudinal` 按长期跟踪研究处理，`transient` 按短暂或临时处理。正式题库更新为 `vocabulary-anchor-bank-555.json`；剩余 45 条 14K–20K 候选继续禁止计分。
- 2026-07-28 完成 15 个 12K 人工锚点，覆盖 `loot` 至 `mundane`，补齐该频段 15 条目标；手工编写 45 个同词性英文干扰项。`loot` 按赃物名词处理，`ecstasy` 按狂喜处理，`compose` 按组成处理，`soluble` 按可溶处理，`mundane` 按平凡日常处理。正式题库更新为 `vocabulary-anchor-bank-540.json`；剩余 60 条 13K–20K 候选继续禁止计分。
- 2026-07-28 完成 15 个 11K 人工锚点，覆盖 `subsidy` 至 `brag`，补齐该频段 15 条目标；手工编写 45 个同词性英文干扰项。`distortion` 按事实或数据歪曲处理，`maneuver` 按操纵移动或驾驶处理，`vomit` 按动词处理，`dedicate` 按专用于或拨出处理，`repression` 按政治镇压或压制处理。正式题库更新为 `vocabulary-anchor-bank-525.json`；剩余 75 条 12K–20K 候选继续禁止计分。
- 2026-07-28 完成 15 个 10K 人工锚点，覆盖 `symptom` 至 `consulate`，补齐该频段 15 条目标；手工编写 45 个同词性英文干扰项。`affinity` 按天然亲和或适配处理，`adolescent` 按青少年名词处理，`discourage` 按阻止或使打消念头处理，`purge` 按清除旧文件或记录处理。正式题库更新为 `vocabulary-anchor-bank-510.json`；剩余 90 条 11K–20K 候选继续禁止计分。
- 2026-07-28 完成 15 个 9K 人工锚点，覆盖 `plantation` 至 `erosion`，补齐该频段 15 条目标；手工编写 45 个同词性英文干扰项。`diplomacy` 按得体沟通处理，`addict` 按成瘾者名词处理，`spur` 按促进动词处理，`fuzzy` 按图像或文字不清楚处理。正式题库更新为 `vocabulary-anchor-bank-495.json`；剩余 105 条 10K–20K 候选继续禁止计分。
- 2026-07-28 完成 50 个 8K 人工锚点，覆盖 `eclipse` 至 `insecure`，补齐该频段至 60 条；手工编写 150 个同词性英文干扰项。正式题库更新为 `vocabulary-anchor-bank-480.json`，1K–8K 高密度核心区全部完成；剩余 120 条 9K–20K 候选继续禁止计分。
- 完成 50 个 6K 人工锚点，覆盖 `passport` 至 `altitude`，补齐该频段至 60 条；手工编写 150 个同词性英文干扰项，并对 `passive`、`crude`、`archive`、`conjunction` 等多义词按当前语境收窄。正式题库更新为 `vocabulary-anchor-bank-380.json`；剩余 120 条候选继续禁止计分。下一批从 9K 开始。
- 完成 36 个 5K 人工锚点，覆盖 `emotion` 至 `medieval`；按当前语境收窄多义词词性和义项，手工编写 108 个同词性英文干扰项。`spray`、`retreat` 按动词，`jam` 按交通拥堵名词，`dairy` 按当前语境的形容词处理。正式题库更新为 `vocabulary-anchor-bank-330.json`，1K–5K 各 60 条；剩余 270 条候选保持冻结顺序、`candidate-unreviewed` 和禁止计分。下一批从 6K 开始。

- 完成 36 个 4K 人工锚点，覆盖 `ambassador` 至 `privilege`；全部题目人工收窄当前语境义项，并手工编写 108 个同词性英文干扰项。`beneath` 保持正确介词词性，使用人工介词干扰项，不为凑数错误改词性。正式题库更新为 `vocabulary-anchor-bank-294.json`，1K–4K 各 60 条；剩余 306 条候选保持冻结顺序、`candidate-unreviewed` 和禁止计分。下一批从 5K 开始。

- 删除每日词汇自测中无信息价值的“下一题”按钮：选择答案后与正式词汇 CAT 一致，立即进入下一题，不显示对错停留状态；最后一题直接进入结果页。新增 1 项交互回归测试。

- 拆分快速定位与词汇量精测：快速定位独占 15 道 Yes/No＋非词路线并持久化起点；词汇量精测直接进入英文语境释义 CAT，最近 120 天定位结果只用于初始能力估计，不重复出题、不计入正式样本。旧版停在精测路由阶段的草稿自动失效。新增 2 项模式隔离与起点复用测试。

- 修复测试进行中顶部返回层级：第一次返回只退出当前题目并回到五种模式的测试中心主页，同时保留未完成草稿；从测试中心主页再次返回才进入学习首页。新增 1 项导航层级回归测试。

- 修复测试中心被未完成草稿直接劫持的问题：进入入口始终先显示五种测试模式；未完成测试只在推荐卡中提供“继续上次测试”，用户仍可直接选择其他模式重新开始。新增 3 项入口状态回归测试。

- 修复 Pages 已部署但旧 PWA 页面继续显示的问题：每次注册时主动检查新 Service Worker，新版本接管后自动刷新一次；首页与测试中心直接显示当前 258 个已审核锚点，避免用户无法判断线上版本。

- 完成 36 个 3K 人工锚点，覆盖 `recommend` 至 `journalist`；多义词按教学语境收窄，例如 `outstanding=尚未支付的`、`avenue=途径`、`associate=把……联系起来`。
- 正式题库更新为 `vocabulary-anchor-bank-258.json`；1K–3K 各 60 条，剩余 342 条候选重新生成并继续保持 `candidate-unreviewed`、`scoringEligible: false`。
- 新增 3K 批次门禁；专项测试 23/23、全量自动测试 222/222、生产构建和本地浏览器验证均通过，既有 IndexedDB 路由进度未清除，控制台无错误。
- 3K 批次以提交 `bb4f3cb` 发布，GitHub Actions `30253885868` 成功；Pages 线上核对正式题库 258、剩余候选 342、3K 候选 0、`scoringEligible: false`。下一批从 4K 频段继续。

- 完成 36 个 2K 人工锚点，覆盖 `traditional` 至 `capable`；多义词按教学语境收窄，例如 `draw=吸引`、`patient=病人`、`perform=执行`、`novel=小说`。
- 正式题库更新为 `vocabulary-anchor-bank-222.json`；1K、2K 各 60 条，剩余 378 条候选重新生成并继续保持 `candidate-unreviewed`、`scoringEligible: false`。
- 新增 2K 批次门禁，检查人工词性、聚焦中文义、英文释义、四个唯一英文选项、四个唯一中文选项、同词性干扰项和多义词语境一致性。
- 专项测试 21/21、全量自动测试 220/220、生产构建和本地浏览器验证均通过；测试中心可以恢复未完成路由，控制台无错误。
- 2K 批次以提交 `a6a2ceb` 发布，GitHub Actions `30245989717` 成功；Pages 线上核对正式题库 222、剩余候选 378、2K 候选 0、`scoringEligible: false`。下一批从 3K 频段继续。

## 2026-07-24 工作日志

- M1 已以提交 `6864508` 发布；M2 已以提交 `76cf9f4` 发布；测试中心命名、层级和五题型隔离已以提交 `a6bf5a2` 发布。
- 已完成 600 锚点的精确配额冻结：现有 150 个 M2 锚点，加首批 36 个 1K 人工锚点和 414 个待审核候选。最终分布为 1K–8K 各 60、9K–14K 各 15、15K–20K 各 5。
- 最初 450 个候选全部来自教学词与 20K 索引的一对一直接映射，具备可核验中文义、目标词语境和搭配；候选 ID、计划锚点 ID、词族、教学词和词形均唯一；完成 1K 首批后剩余 414 个。
- 剩余候选状态全部是 `candidate-unreviewed`，`scoringEligible` 为 `false`，运行时 CAT 未导入该文件。今天先保持 450 条均不可计分，随后仅将真正完成人工定义与选项审核的 1K 频段 36 条转为 `item-authored` 并接入 CAT；剩余 414 条仍不可计分。
- 新增 7 项自动门禁，覆盖精确频段配额、不可计分状态、唯一映射、语境与占位内容、运行时隔离、频段内覆盖和首批人工题目完整性；全量自动测试为 218 项。
- 今天遇到并修复了候选编号按频段重复的问题；修复后 450 个候选编号全部唯一。随后增加频段内分层抽样，20 个频段均覆盖自身跨度，不再只取每层开头的词。1K 首批 36 条已人工审核并进入 `vocabulary-anchor-bank-186.json`。
- GitHub Actions 已成功：M1 `30069125985`、M2 `30072263627`、测试中心 UI `30073378388`、M3 候选冻结 `30074525976`、频段内分层抽样 `30074857451`、1K 首批人工锚点 `30075536813`。对应 M3 提交为 `361808b`、`f0ffe19`、`d5b9312`。
- 家里电脑以 GitHub `main` 为唯一基准。拉取后依次阅读 `AGENTS.md`、`PROJECT_OVERVIEW.md`、本文件，运行 `pnpm install --frozen-lockfile`、`pnpm test`、`pnpm build`，然后从上方“下一次立即执行”的第 1 条继续。
- 2026-08-01 完成 M4 第一工作单元：实现版本化 V0 文本清洗、句子识别、20K 词族映射、句长、词频、覆盖率、未登录词、音节、长词、连接与从属、代词指代、名词化、词汇密度、多样性和语义可用性审计；建立参考语料来源、许可、双标签和固定分割门禁；为现有 128 篇阅读课程生成仅特征快照，全部保持不可计分。自动测试增至 280 项。
- 2026-08-01 完成 M4 合法语料候选批次：从 VOA Learning English 官方公共领域课程中提取 48 篇 Level 1、30 篇 Level 2 和 42 篇高级语法文本；120 个网址和正文哈希全部唯一，第三方新闻社内容为 0，六层人工复核队列各 20 篇，固定分割为 train 84／validation 24／holdout 12。全部仍为 `source-reviewed`，独立人工标签 0，计分合格 0。自动测试增至 285 项。
